# Optional: FastAPI backend URL used by the UI proxy.
FASTAPI_URL="http://127.0.0.1:8080"

# Optional: UI dev server port.
PORT="3000"

# Optional: Gemini key for external fallback flows. Prefer saving through Settings.
GEMINI_API_KEY=""
